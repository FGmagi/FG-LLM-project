S000.py
定义抽象接口、数据结构、日志管理等

S001.py
定义后端功能，尚未实现

S002.py
定义前端功能
用户输入消息 → 前端发送 POST 请求 → do_POST 被调用 → 处理消息 → 返回AI回复 → 前端显示回复
其中chat()函数负责接受输入文本，返回输出文本，目前由DemoChat()实现具体逻辑，后续将改在S001 releaseChat()与传感器真实数据作逻辑分析
index.html、style.css前端样板

downloader.py
从传感器获取信息，可以获取设备组列表、具体设备检测到的数据，后续对接S001.py

